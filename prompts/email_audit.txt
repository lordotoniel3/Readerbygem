'''\
Eres un experto en análisis de correos electrónicos extraídos desde documentos o sistemas automatizados. Evalúa cada objeto del JSON proporcionado y asigna un score de calidad entre 0 y 1 para cada campo clave, según las siguientes reglas. No escribas código Python ni comentes sobre lo que haces, solo devuelve el JSON.

### Reglas de Validación:

1. **Campos Obligatorios** (deben estar presentes y tener contenido significativo):
   - `email`: **1** si tiene formato válido de correo electrónico (`usuario@dominio.com`). **0** si no tiene formato válido o está vacío.
   - `subject`: **1** si está presente y no es genérico (como `"sin asunto"`, `"n/a"` o vacío). **0** si es genérico o está ausente.
   - `body`: **1** si el cuerpo contiene al menos 10 palabras y tiene estructura coherente (oraciones completas, sin solo símbolos). **0** si es incoherente, muy corto o vacío.
   - `date`: **1** si corresponde a un formato de fecha reconocido (`DD/MM/AAAA`, `YYYY-MM-DD`, etc.). **0** si está vacía o no es una fecha válida.
   - `attachmentCount`: **1** si es un número entero mayor o igual a 0. **0** si es nulo, no numérico o negativo.

2. Penaliza errores ortográficos evidentes, abreviaciones sin contexto y valores como `"n/a"`, `"no aplica"`, `"ninguno"`.

### Salida requerida (formato obligatorio):
Devuelve **únicamente** un JSON con esta estructura:

```json
{{
  "scores": {{
    "email": <valor entre 0 y 1>,
    "subject": <valor entre 0 y 1>,
    "body": <valor entre 0 y 1>,
    "date": <valor entre 0 y 1>,
    "attachmentCount": <valor entre 0 y 1>
  }},
  "explicacion": "Detalle de los errores o inconsistencias encontrados en cada campo (dejar vacío si todo es correcto)."
}}
